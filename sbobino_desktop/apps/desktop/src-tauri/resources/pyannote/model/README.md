Place the offline pyannote pipeline contents in this directory.

Expected minimum contents:

- `config.yaml`
- the model files referenced by `config.yaml`

The app treats this directory as valid only when `config.yaml` is present.
